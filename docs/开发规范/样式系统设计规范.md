# 样式系统设计规范

> **版本**: v2.0  
> **更新日期**: 2025-12-28  
> **适用项目**: dhs-atlas（AI 原生工作操作系统）

---

## 一、设计理念

### 1.1 AI 原生约束

dhs-atlas 的样式系统必须满足 AI 原生要求：

| 原则 | 说明 |
|------|------|
| **可预测** | UI 行为必须可预测，AI 能理解组件状态 |
| **可结构化** | 样式必须通过结构化方式定义，非魔法值 |
| **可推断** | 组件语义清晰，AI 能推断交互方式 |
| **无噪音** | 禁止 Emoji、装饰性元素等非功能性视觉 |

### 1.2 设计风格

- **黑白灰极简** — 95% 中性色调
- **功能性强调** — 仅状态指示使用彩色
- **克制的视觉** — 极轻阴影、无装饰渐变
- **4px 基准网格** — 所有间距遵循 4px 倍数

---

## 二、技术架构

### 2.1 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Tailwind CSS | v4 | 工具类样式框架 |
| shadcn/ui | 最新 | 组件库（唯一） |
| CSS Variables | - | 主题 Token |
| oklch | - | 颜色空间 |

### 2.2 文件结构

```
src/
├── app/
│   └── globals.css          # 全局样式 + CSS 变量
├── components/
│   └── ui/                   # shadcn 组件（勿修改）
├── lib/
│   └── utils.ts              # cn() 工具函数
└── styles/                   # 自定义样式（仅必要时）
    └── [component].css
```

### 2.3 导入链路

```
layout.tsx
    ↓ 导入
globals.css
    ↓ 包含
├── Tailwind CSS 基础
├── CSS 变量定义
├── shadcn 主题配置
└── 全局重置样式
```

---

## 三、CSS 变量系统

### 3.1 颜色系统

基于 oklch 颜色空间定义：

```css
:root {
  /* 背景 */
  --background: oklch(1 0 0);
  --foreground: oklch(0.145 0 0);
  
  /* 卡片 */
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.145 0 0);
  
  /* 弹出层 */
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.145 0 0);
  
  /* 主色 */
  --primary: oklch(0.205 0 0);
  --primary-foreground: oklch(0.985 0 0);
  
  /* 次要 */
  --secondary: oklch(0.97 0 0);
  --secondary-foreground: oklch(0.205 0 0);
  
  /* 柔和 */
  --muted: oklch(0.97 0 0);
  --muted-foreground: oklch(0.556 0 0);
  
  /* 强调 */
  --accent: oklch(0.97 0 0);
  --accent-foreground: oklch(0.205 0 0);
  
  /* 危险 */
  --destructive: oklch(0.577 0.245 27.325);
  
  /* 边框/输入 */
  --border: oklch(0.922 0 0);
  --input: oklch(0.922 0 0);
  --ring: oklch(0.708 0 0);
}

.dark {
  --background: oklch(0.145 0 0);
  --foreground: oklch(0.985 0 0);
  /* ... 暗色主题变量 */
}
```

### 3.2 间距系统（4px 基准）

使用 Tailwind 内置间距，基于 4px：

| Tailwind 类 | 值 | 用途 |
|------------|-----|------|
| `p-1`, `m-1` | 4px | 紧凑间距 |
| `p-2`, `m-2` | 8px | 小间距 |
| `p-3`, `m-3` | 12px | 中等间距 |
| `p-4`, `m-4` | 16px | 标准间距 |
| `p-5`, `m-5` | 20px | 大间距 |
| `p-6`, `m-6` | 24px | 区块间距 |
| `p-8`, `m-8` | 32px | 页面间距 |

### 3.3 圆角系统

```css
:root {
  --radius: 0.625rem; /* 10px - shadcn 默认 */
}
```

| Tailwind 类 | 用途 |
|------------|------|
| `rounded-sm` | 小圆角（4px） |
| `rounded` | 默认圆角（6px） |
| `rounded-md` | 中等圆角（8px） |
| `rounded-lg` | 大圆角（10px） |
| `rounded-xl` | 特大圆角（12px） |
| `rounded-full` | 圆形 |

### 3.4 阴影系统（极简）

```tsx
// shadcn 默认阴影 - 极轻
className="shadow-sm"   // 轻微阴影
className="shadow"      // 默认阴影
className="shadow-md"   // 中等阴影
className="shadow-lg"   // 大阴影（慎用）
```

---

## 四、组件样式规范

### 4.1 按钮

```tsx
// 主要操作
<Button variant="default">确定</Button>

// 次要操作
<Button variant="outline">取消</Button>
<Button variant="secondary">次要</Button>

// 危险操作
<Button variant="destructive">删除</Button>

// 文本/链接
<Button variant="ghost">更多</Button>
<Button variant="link">查看</Button>

// 尺寸
<Button size="sm">小</Button>
<Button size="default">默认</Button>
<Button size="lg">大</Button>
<Button size="icon"><Icon /></Button>
```

### 4.2 卡片

```tsx
<Card>
  <CardHeader>
    <CardTitle>标题</CardTitle>
    <CardDescription>描述</CardDescription>
  </CardHeader>
  <CardContent>
    内容
  </CardContent>
  <CardFooter>
    <Button>操作</Button>
  </CardFooter>
</Card>
```

### 4.3 表单

```tsx
<form className="space-y-4">
  <div className="space-y-2">
    <Label htmlFor="field">字段名称</Label>
    <Input id="field" placeholder="请输入" />
    <p className="text-sm text-muted-foreground">帮助文本</p>
  </div>
</form>
```

### 4.4 表格

```tsx
<Table>
  <TableHeader>
    <TableRow>
      <TableHead className="w-[200px]">列1</TableHead>
      <TableHead>列2</TableHead>
      <TableHead className="text-right">操作</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell className="font-medium">数据</TableCell>
      <TableCell className="text-muted-foreground">次要</TableCell>
      <TableCell className="text-right">
        <Button variant="ghost" size="sm">编辑</Button>
      </TableCell>
    </TableRow>
  </TableBody>
</Table>
```

### 4.5 状态徽章

```tsx
// 成功
<Badge className="bg-green-100 text-green-700 hover:bg-green-100">
  已完成
</Badge>

// 警告
<Badge className="bg-yellow-100 text-yellow-700 hover:bg-yellow-100">
  进行中
</Badge>

// 错误
<Badge className="bg-red-100 text-red-700 hover:bg-red-100">
  失败
</Badge>

// 信息
<Badge className="bg-blue-100 text-blue-700 hover:bg-blue-100">
  待处理
</Badge>

// 中性
<Badge variant="secondary">
  草稿
</Badge>
```

---

## 五、布局系统

### 5.1 页面布局

```tsx
// 标准页面结构
<div className="min-h-screen bg-background">
  {/* 页面容器 */}
  <div className="container mx-auto p-6">
    {/* 页面标题区 */}
    <div className="flex items-center justify-between mb-6">
      <h1 className="text-2xl font-semibold">页面标题</h1>
      <Button>操作</Button>
    </div>
    
    {/* 内容区 */}
    <Card>
      <CardContent className="p-6">
        内容
      </CardContent>
    </Card>
  </div>
</div>
```

### 5.2 栅格布局

```tsx
// 等宽多列
<div className="grid grid-cols-4 gap-4">
  <Card>1</Card>
  <Card>2</Card>
  <Card>3</Card>
  <Card>4</Card>
</div>

// 响应式
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
  <Card>1</Card>
  <Card>2</Card>
  <Card>3</Card>
  <Card>4</Card>
</div>

// 不等宽
<div className="grid grid-cols-12 gap-4">
  <div className="col-span-8">主内容</div>
  <div className="col-span-4">侧边栏</div>
</div>
```

### 5.3 Flex 布局

```tsx
// 水平居中对齐
<div className="flex items-center gap-4">
  <Icon />
  <span>文本</span>
</div>

// 两端对齐
<div className="flex items-center justify-between">
  <span>左侧</span>
  <span>右侧</span>
</div>

// 垂直布局
<div className="flex flex-col gap-4">
  <div>项目1</div>
  <div>项目2</div>
</div>
```

---

## 六、响应式规范

### 6.1 断点定义

| 断点 | 宽度 | 用途 |
|------|------|------|
| `sm` | 640px | 小屏幕 |
| `md` | 768px | 平板 |
| `lg` | 1024px | 桌面 |
| `xl` | 1280px | 大屏 |
| `2xl` | 1536px | 超大屏 |

### 6.2 响应式写法

```tsx
// 移动优先
<div className="p-4 md:p-6 lg:p-8">
  <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
    {/* 内容 */}
  </div>
</div>

// 隐藏/显示
<div className="hidden md:block">仅桌面显示</div>
<div className="md:hidden">仅移动端显示</div>
```

---

## 七、禁止事项

### 7.1 绝对禁止

| 禁止项 | 原因 | 替代方案 |
|-------|------|---------|
| Emoji | AI 无法解析 | Lucide Icons |
| 内联样式 | 不可维护 | Tailwind 类 |
| 硬编码颜色 | 无法主题切换 | CSS 变量 |
| 硬编码间距 | 不一致 | Tailwind 间距 |
| 装饰性渐变 | 视觉噪音 | 纯色背景 |
| 假进度条 | 欺骗用户 | 真实进度 |
| 魔改 shadcn | 破坏一致性 | className 扩展 |

### 7.2 审查检查

```tsx
// ❌ 错误示例
<div style={{ padding: '16px', color: '#333' }}>  // 内联样式
<div className="bg-[#f5f5f5]">                     // 硬编码颜色
<span>🎉 成功</span>                                // Emoji

// ✅ 正确示例
<div className="p-4 text-foreground">
<div className="bg-muted">
<span><CheckCircle className="h-4 w-4" /> 成功</span>
```

---

## 八、工具与集成

### 8.1 cn() 工具函数

```tsx
import { cn } from "@/lib/utils"

// 合并类名
<div className={cn(
  "base-class",
  isActive && "active-class",
  variant === "primary" && "primary-class"
)} />
```

### 8.2 VS Code 插件

```json
{
  "recommendations": [
    "bradlc.vscode-tailwindcss",
    "csstools.postcss"
  ]
}
```

### 8.3 Tailwind 配置

```js
// tailwind.config.js
module.exports = {
  darkMode: ["class"],
  content: [
    './pages/**/*.{ts,tsx}',
    './components/**/*.{ts,tsx}',
    './app/**/*.{ts,tsx}',
    './src/**/*.{ts,tsx}',
  ],
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        // ... 更多颜色
      },
    },
  },
}
```

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v2.0 | 2025-12-28 | 重构为 dhs-atlas shadcn/ui + Tailwind 规范 |
| v1.0 | 2025-08-28 | 初始版本 |

---

**文档维护**: dhs-atlas 开发团队  
**最高约束**: [开发伦理与系统操作宪章](../开发伦理与系统操作宪章-最高指示.md)
