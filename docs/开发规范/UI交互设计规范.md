# dhs-atlas UI 交互设计规范（v1.0）

> **版本**: v1.0  
> **创建日期**: 2025-12-28  
> **文档性质**: 强制规范  
> **适用范围**: 所有前端应用（console / platform / portal / platform-web）

---

## 一、三条底层准则（不可违背）

### 1.1 真实交互准则

**核心原则**：每一个按钮必须对应一个后端可审计的状态变化。

| 要求 | 说明 |
|------|------|
| **禁止拟合交互** | 禁止"点击后弹个 toast 就算完成"的假交互 |
| **必须产出结果** | 所有关键动作必须产出 Event Log + Result Object |
| **必须可解释** | 用户能看到"发生了什么" |
| **必须可回溯** | 管理员/AI 能追溯"谁在什么时候做了什么" |

**合规示例**：

```typescript
// ✅ 正确：点击后产生真实状态变更
async function handleAcceptDeliverable(deliverableId: string) {
  const result = await api.POST('/deliverables/{id}/accept', {
    params: { path: { id: deliverableId } },
    body: { reason: selectedReason },
  });
  
  // 结果包含：
  // - event_id: 事件日志 ID
  // - new_status: 新状态
  // - next_action: 下一步可选动作
  
  if (result.data) {
    // 刷新页面状态，而不是前端拟合
    await refetch();
  }
}

// ❌ 错误：前端拟合状态
function handleAcceptDeliverable(deliverableId: string) {
  setStatus('accepted');  // 假状态
  toast.success('验收成功'); // 假反馈
}
```

**验收条款**：

- [ ] 每个按钮点击后必须调用后端 API
- [ ] API 返回必须包含 `event_id` 或状态变更证明
- [ ] 禁止前端直接修改状态后显示"成功"

---

### 1.2 单一交互协议准则

**核心原则**：所有模块必须用同一套"交互原语"构建。

**六大交互原语**：

| 原语 | 用途 | 组件名 |
|------|------|--------|
| **List** | 列表展示、筛选、批量操作 | `EntityList` |
| **Detail** | 详情展示、状态机驱动 | `EntityDetail` |
| **Form** | 创建/编辑表单 | `EntityForm` |
| **Wizard** | 分步流程 | `StepWizard` |
| **Review** | 审核/确认/对比 | `ReviewPanel` |
| **Timeline** | 任务/运行日志 | `ActivityTimeline` |

**原语映射表**：

| 场景 | 使用原语 |
|------|---------|
| 客户列表 | List |
| 客户详情 | Detail + Timeline |
| 创建客户 | Form |
| 创建报价（多步骤） | Wizard |
| 合同预检 | Review + Timeline |
| 交付验收 | Detail + Review |

**验收条款**：

- [ ] 所有页面必须基于六大原语构建
- [ ] 禁止为单个页面自创交互模式
- [ ] AI 必须能通过原语 API 完成所有操作

---

### 1.3 AI 可操作准则

**核心原则**：UI 必须可被 AI 通过"组件协议"驱动。

**要求**：

| 要求 | 说明 |
|------|------|
| **Action Schema** | 所有交互组件暴露统一的 action schema |
| **无业务逻辑** | 前端不埋业务逻辑，逻辑在后端 Tool/Workflow |
| **可程序化** | 任何人能做的操作，AI 必须能做 |

**Action Schema 示例**：

```typescript
// 每个页面/组件必须声明可执行的 Action
interface ServiceListActions {
  // 创建服务
  'service.create': {
    input: CreateServiceInput;
    output: Service;
  };
  
  // 更新服务状态
  'service.updateStatus': {
    input: { id: string; status: ServiceStatus };
    output: Service;
  };
  
  // 删除服务
  'service.delete': {
    input: { id: string };
    output: void;
  };
  
  // 批量操作
  'service.batchArchive': {
    input: { ids: string[] };
    output: { success: number; failed: number };
  };
}

// 组件暴露 executeAction 方法
interface ActionableComponent<T extends Record<string, ActionDef>> {
  executeAction<K extends keyof T>(
    action: K,
    input: T[K]['input']
  ): Promise<T[K]['output']>;
  
  getAvailableActions(): (keyof T)[];
}
```

**验收条款**：

- [ ] 每个页面必须声明 Action Schema
- [ ] AI 可以通过 `executeAction` 完成所有操作
- [ ] 前端不包含任何业务计算逻辑

---

## 二、信息架构与导航

### 2.1 导航分层原则

```
┌─────────────────────────────────────────────────────────────────────┐
│                         导航分层架构                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────┐                                                  │
│   │   左侧导航   │  ──►  只做"域"（业务领域）                        │
│   │  Sidebar    │       CRM / 服务 / 报价 / 项目 / 财务 / ...      │
│   └─────────────┘                                                  │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐  │
│   │                        页内导航                              │  │
│   │                                                             │  │
│   │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │  │
│   │  │  Tabs   │  │ Status  │  │ Breadcrumb│ │Sub-menu │       │  │
│   │  │  标签页  │  │  Filter │  │   面包屑  │ │ 子菜单  │       │  │
│   │  └─────────┘  └─────────┘  └─────────┘  └─────────┘       │  │
│   │                                                             │  │
│   │  做"流程"（状态机推进）                                      │  │
│   │  draft → sent → accepted → in_progress → completed         │  │
│   │                                                             │  │
│   └─────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 用户后台导航结构

```typescript
// 左侧导航 - 只做域
export const sidebarNavigation = [
  { 
    title: '工作台', 
    icon: 'LayoutDashboard', 
    href: '/dashboard' 
  },
  { 
    title: '客户管理', 
    icon: 'Users', 
    href: '/customers',
    children: [
      { title: '客户列表', href: '/customers' },
      { title: '联系人', href: '/contacts' },
    ],
  },
  { 
    title: '服务管理', 
    icon: 'Package', 
    href: '/services',
    children: [
      { title: '服务列表', href: '/services' },
      { title: '服务分类', href: '/service-categories' },
    ],
  },
  { 
    title: '报价菜单', 
    icon: 'Menu', 
    href: '/quote-templates' 
  },
  { 
    title: '订单管理', 
    icon: 'ShoppingCart', 
    href: '/orders' 
  },
  { 
    title: '项目交付', 
    icon: 'FolderKanban', 
    href: '/projects',
    children: [
      { title: '项目列表', href: '/projects' },
      { title: '交付管理', href: '/deliverables' },
    ],
  },
  { 
    title: '合同管理', 
    icon: 'FileText', 
    href: '/contracts',
    children: [
      { title: '合同列表', href: '/contracts' },
      { title: '合同范本', href: '/contract-templates' },
      { title: '合同预检', href: '/contract-reviews' },
    ],
  },
  { 
    title: '财务管理', 
    icon: 'Wallet', 
    href: '/finance',
    children: [
      { title: '结算单', href: '/settlements' },
      { title: '发票管理', href: '/invoices' },
      { title: '收入报表', href: '/revenue' },
    ],
  },
  { 
    title: '文件管理', 
    icon: 'FolderOpen', 
    href: '/files' 
  },
  { 
    title: '内容管理', 
    icon: 'Globe', 
    href: '/cms',
    children: [
      { title: '页面管理', href: '/pages' },
      { title: '文章管理', href: '/articles' },
      { title: '官网设置', href: '/website-settings' },
    ],
  },
  { 
    title: '组织设置', 
    icon: 'Building2', 
    href: '/organization',
    children: [
      { title: '企业信息', href: '/enterprise' },
      { title: '部门管理', href: '/departments' },
      { title: '员工管理', href: '/employees' },
      { title: '权限管理', href: '/permissions' },
    ],
  },
  { 
    title: '系统设置', 
    icon: 'Settings', 
    href: '/settings' 
  },
];
```

### 2.3 主清单页标准

每个域必须有"主清单页"作为控制塔。

**必备元素**：

| 元素 | 必要性 | 说明 |
|------|--------|------|
| 状态筛选 | **必备** | Tab 或 Filter，按状态机状态筛选 |
| 关键指标 | 可选 | 统计卡片（数量、金额等） |
| 快速动作 | **必备** | 新建、批量操作、导出 |
| 搜索 | **必备** | 关键字搜索 |
| 高级筛选 | 可选 | 多条件组合筛选 |
| 统一空态 | **必备** | 指导用户下一步 |

---

## 三、详情页标准结构

### 3.1 标准布局

```
┌─────────────────────────────────────────────────────────────────────┐
│                         详情页标准结构                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  顶部：标题 + 状态 Chip + 主动作（Primary CTA）              │   │
│   │                                                             │   │
│   │  [客户名称]                        [已激活]     [创建订单]   │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│   ┌──────────────────────────┐  ┌──────────────────────────────┐   │
│   │                          │  │                              │   │
│   │   左侧：时间线            │  │   右侧：对象信息              │   │
│   │   (Timeline)             │  │   (Object Info)              │   │
│   │                          │  │                              │   │
│   │   ┌────────────────────┐ │  │   基本信息                   │   │
│   │   │ 2025-01-15 10:30   │ │  │   ├── 名称：XXX             │   │
│   │   │ 张三 创建了客户     │ │  │   ├── 类型：企业            │   │
│   │   └────────────────────┘ │  │   └── 级别：VIP             │   │
│   │                          │  │                              │   │
│   │   ┌────────────────────┐ │  │   联系信息                   │   │
│   │   │ 2025-01-16 14:20   │ │  │   ├── 联系人：李四          │   │
│   │   │ AI 生成了报价建议   │ │  │   └── 电话：138****        │   │
│   │   └────────────────────┘ │  │                              │   │
│   │                          │  │   关联对象                   │   │
│   │   ┌────────────────────┐ │  │   ├── 项目：3 个            │   │
│   │   │ 2025-01-17 09:00   │ │  │   └── 订单：5 个            │   │
│   │   │ 客户确认了报价      │ │  │                              │   │
│   │   └────────────────────┘ │  │   附件                       │   │
│   │                          │  │   └── 营业执照.pdf          │   │
│   │                          │  │                              │   │
│   └──────────────────────────┘  └──────────────────────────────┘   │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  底部：讨论/批注（如果需要）                                  │   │
│   │                                                             │   │
│   │  [输入框...]                                    [发送评论]   │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 时间线（Timeline）

**核心价值**：Timeline 是"真实感"和"可信感"的来源。

**时间线条目类型**：

| 类型 | 图标 | 说明 |
|------|------|------|
| `created` | ➕ | 对象创建 |
| `updated` | ✏️ | 字段变更 |
| `status_changed` | 🔄 | 状态变更 |
| `ai_action` | 🤖 | AI 工具运行 |
| `comment` | 💬 | 用户评论 |
| `file_uploaded` | 📎 | 文件上传 |
| `approval` | ✅ | 审批通过 |
| `rejection` | ❌ | 审批拒绝 |

**时间线数据结构**：

```typescript
interface TimelineEntry {
  id: string;
  timestamp: Date;
  
  // 事件类型
  type: 'created' | 'updated' | 'status_changed' | 'ai_action' | 
        'comment' | 'file_uploaded' | 'approval' | 'rejection';
  
  // 执行者
  actor: {
    type: 'user' | 'system' | 'ai';
    id?: string;
    name: string;
    avatar?: string;
  };
  
  // 事件描述
  title: string;
  description?: string;
  
  // 详情（可选）
  details?: {
    // 字段变更
    changes?: Array<{
      field: string;
      oldValue: unknown;
      newValue: unknown;
    }>;
    
    // AI 工具运行
    toolExecution?: {
      toolId: string;
      toolName: string;
      input: unknown;
      output: unknown;
      duration: number;
    };
    
    // 文件
    files?: Array<{
      id: string;
      name: string;
      url: string;
    }>;
  };
}
```

### 3.3 状态机驱动 CTA

**核心原则**：

| 原则 | 说明 |
|------|------|
| **唯一主动作** | 页面上任何时候只出现 1 个主动作 |
| **次要折叠** | 次要动作折叠到 overflow menu |
| **可解释** | 主动作必须能解释：为什么可以点、点了会怎样、失败会怎样 |

**状态-动作映射示例（订单）**：

```typescript
interface OrderActions {
  draft: {
    primary: { action: 'submit', label: '提交订单' };
    secondary: ['edit', 'delete'];
  };
  pending_confirm: {
    primary: { action: 'confirm', label: '确认订单' };
    secondary: ['cancel', 'edit'];
  };
  confirmed: {
    primary: { action: 'create_project', label: '创建项目' };
    secondary: ['cancel'];
  };
  in_progress: {
    primary: { action: 'view_project', label: '查看项目' };
    secondary: ['cancel'];
  };
  completed: {
    primary: { action: 'create_settlement', label: '创建结算单' };
    secondary: ['view_project'];
  };
  cancelled: {
    primary: null;
    secondary: ['restore'];
  };
}

// CTA 按钮组件
function EntityCTA({ entity, actions }: Props) {
  const currentActions = actions[entity.status];
  
  return (
    <div className="flex items-center gap-2">
      {currentActions.primary && (
        <Button onClick={() => executeAction(currentActions.primary.action)}>
          {currentActions.primary.label}
        </Button>
      )}
      
      {currentActions.secondary.length > 0 && (
        <DropdownMenu>
          <DropdownMenuTrigger asChild>
            <Button variant="outline" size="icon">
              <MoreHorizontal className="h-4 w-4" />
            </Button>
          </DropdownMenuTrigger>
          <DropdownMenuContent>
            {currentActions.secondary.map(action => (
              <DropdownMenuItem 
                key={action}
                onClick={() => executeAction(action)}
              >
                {getActionLabel(action)}
              </DropdownMenuItem>
            ))}
          </DropdownMenuContent>
        </DropdownMenu>
      )}
    </div>
  );
}
```

---

## 四、核心链路交互设计

### 4.1 Quote 菜单编辑体验

**目标**：让用户像"搭配套餐"一样组合服务，而不是填表。

```
┌─────────────────────────────────────────────────────────────────────┐
│                       报价菜单编辑器                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌────────────┐  ┌────────────────────────┐  ┌────────────────┐   │
│   │            │  │                        │  │                │   │
│   │  服务目录   │  │      已选服务项         │  │   价格汇总     │   │
│   │            │  │                        │  │                │   │
│   │  [搜索...]  │  │  ┌──────────────────┐  │  │  小计: ¥8,000 │   │
│   │            │  │  │ Logo 设计         │  │  │                │   │
│   │  ▾ 视觉设计 │  │  │ 数量: 1  ¥3,000  │  │  │  折扣: -¥800  │   │
│   │    Logo    │  │  │ [配置] [删除]     │  │  │  (会员9折)    │   │
│   │    VI      │  │  └──────────────────┘  │  │                │   │
│   │    海报    │  │                        │  │  税费: ¥432    │   │
│   │            │  │  ┌──────────────────┐  │  │                │   │
│   │  ▾ 内容服务 │  │  │ 文案撰写         │  │  │  ───────────  │   │
│   │    文案    │  │  │ 数量: 5  ¥5,000  │  │  │                │   │
│   │    翻译    │  │  │ [配置] [删除]     │  │  │  总计: ¥7,632 │   │
│   │            │  │  └──────────────────┘  │  │                │   │
│   │            │  │                        │  │  ───────────   │   │
│   │            │  │  [可拖拽排序]          │  │                │   │
│   │            │  │                        │  │  [价格明细 >]  │   │
│   │            │  │                        │  │                │   │
│   └────────────┘  └────────────────────────┘  └────────────────┘   │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  底部操作栏                                                  │   │
│   │                                                             │   │
│   │  [保存草稿]  [预览]  [导出 PDF]  [分享链接]  [绑定客户 >]    │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**关键约束**：

| 约束 | 说明 |
|------|------|
| **价格计算必须来自后端** | 前端不做价格计算，避免拟合 |
| **每次变更都写事件** | `quote.item_added` / `quote.item_removed` / `quote.updated` |
| **支持版本化** | 可以回溯到任意历史版本 |

**交互 Action Schema**：

```typescript
interface QuoteEditorActions {
  'quote.addItem': {
    input: { serviceId: string; quantity?: number };
    output: QuoteItem;
  };
  'quote.removeItem': {
    input: { itemId: string };
    output: void;
  };
  'quote.updateItem': {
    input: { itemId: string; quantity?: number; options?: ItemOptions };
    output: QuoteItem;
  };
  'quote.reorderItems': {
    input: { itemIds: string[] };
    output: void;
  };
  'quote.saveDraft': {
    input: {};
    output: Quote;
  };
  'quote.publish': {
    input: {};
    output: Quote;
  };
  'quote.bindCustomer': {
    input: { customerId: string };
    output: Quote;
  };
}
```

### 4.2 从菜单下单（Order）

**流程**：

```
选择菜单 → 配置参数 → 生成订单 → 自动创建项目草案
```

**参数配置界面**：

```
┌─────────────────────────────────────────────────────────────────────┐
│                         创建订单                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   Step 1: 选择菜单                                                  │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  [已选] 标准视觉设计套餐   ¥15,000                           │   │
│   │                                                             │   │
│   │  包含：Logo设计 x1, VI设计 x1, 名片设计 x1                   │   │
│   │  [更换菜单]                                                  │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│   Step 2: 本次参数                                                  │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                                                             │   │
│   │  客户：[选择客户 v]                                          │   │
│   │                                                             │   │
│   │  交付时间：[2025-02-15]                                      │   │
│   │                                                             │   │
│   │  备注：[输入备注...]                                         │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│   Step 3: 预览（订单生成后自动创建）                                │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                                                             │   │
│   │  ☑ 项目：视觉设计项目-客户名称                               │   │
│   │     里程碑：                                                 │   │
│   │     ├── 需求确认（3天）                                     │   │
│   │     ├── 初稿设计（7天）                                     │   │
│   │     ├── 修改调整（5天）                                     │   │
│   │     └── 最终交付（2天）                                     │   │
│   │                                                             │   │
│   │  ☑ 交付包结构：                                             │   │
│   │     ├── Logo 源文件                                         │   │
│   │     ├── VI 手册                                             │   │
│   │     └── 名片设计稿                                          │   │
│   │                                                             │   │
│   │  □ 需客户确认事项：                                          │   │
│   │     └── 确认设计风格偏好                                    │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │                            [取消]    [创建订单并启动项目]    │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 4.3 Project/Delivery 体验

**双视图模式**：

| 视图 | 适用场景 |
|------|---------|
| **时间线视图** | 关注进度、历史记录 |
| **看板视图** | 关注当前状态、批量操作 |

**交付包验收**：

```
┌─────────────────────────────────────────────────────────────────────┐
│                       交付物验收                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   交付包：Logo 设计终稿                          [待验收]           │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  文件列表                                                    │   │
│   │                                                             │   │
│   │  ☑ Logo_final.ai        2.3MB   [预览] [下载]               │   │
│   │  ☑ Logo_final.png       156KB   [预览] [下载]               │   │
│   │  ☑ Logo_使用规范.pdf    890KB   [预览] [下载]               │   │
│   │                                                             │   │
│   │                                    [全部下载]               │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  验收操作                                                    │   │
│   │                                                             │   │
│   │  ○ 验收通过                                                 │   │
│   │                                                             │   │
│   │  ○ 需要修改                                                 │   │
│   │    └── 原因（选择）：                                       │   │
│   │        □ 质量不符合预期                                     │   │
│   │        □ 内容不完整                                         │   │
│   │        □ 与需求不符                                         │   │
│   │        □ 格式/规格问题                                      │   │
│   │        □ 其他                                               │   │
│   │    └── 补充说明：[输入...]                                  │   │
│   │                                                             │   │
│   │  ○ 拒绝交付                                                 │   │
│   │    └── 原因（必填）：[选择...]                               │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│                                         [取消]    [确认验收结果]    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 五、合同预检交互

### 5.1 任务型工作台结构

**核心原则**：预检是"任务型工作台"，不是一次性输出报告。

```
┌─────────────────────────────────────────────────────────────────────┐
│                       合同预检工作台                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   合同：服务合同-客户A.pdf                    [进行中]  [完成预检]  │
│                                                                     │
│   ┌────────────────────────┐  ┌────────────────────────────────┐   │
│   │                        │  │                                │   │
│   │   风险项列表            │  │   风险详情                     │   │
│   │                        │  │                                │   │
│   │   筛选：               │  │   ┌────────────────────────┐   │   │
│   │   [全部 v] [未处理 v]  │  │   │ 风险等级：高            │   │   │
│   │                        │  │   │ 条款位置：第5.2条       │   │   │
│   │   ──────────────────   │  │   └────────────────────────┘   │   │
│   │                        │  │                                │   │
│   │   🔴 违约金条款过高     │  │   证据（原文）                 │   │
│   │      第5.2条 [未处理]  │  │   ┌────────────────────────┐   │   │
│   │                        │  │   │ "乙方违约须支付合同    │   │   │
│   │   🟡 付款条件模糊       │  │   │  总额200%的违约金"     │   │   │
│   │      第3.1条 [已处理]  │  │   └────────────────────────┘   │   │
│   │                        │  │                                │   │
│   │   🟡 交付标准不明确     │  │   风险解释                     │   │
│   │      第4.3条 [未处理]  │  │   违约金比例过高，超出行业      │   │
│   │                        │  │   惯例（通常为10-30%），可能    │   │
│   │   🟢 知识产权归属       │  │   被认定为无效条款。            │   │
│   │      第6.1条 [已处理]  │  │                                │   │
│   │                        │  │   修复方案                     │   │
│   │                        │  │   ┌────────────────────────┐   │   │
│   │                        │  │   │ A. 改为30%（推荐）     │   │   │
│   │                        │  │   │    "...支付合同总额    │   │   │
│   │                        │  │   │     30%的违约金"       │   │   │
│   │                        │  │   ├────────────────────────┤   │   │
│   │                        │  │   │ B. 改为50%             │   │   │
│   │                        │  │   │    "...支付合同总额    │   │   │
│   │                        │  │   │     50%的违约金"       │   │   │
│   │                        │  │   ├────────────────────────┤   │   │
│   │                        │  │   │ C. 删除此条款          │   │   │
│   │                        │  │   └────────────────────────┘   │   │
│   │                        │  │                                │   │
│   │                        │  │   [应用修复 A]  [忽略此风险]   │   │
│   │                        │  │                                │   │
│   └────────────────────────┘  └────────────────────────────────┘   │
│                                                                     │
│   ┌─────────────────────────────────────────────────────────────┐   │
│   │  运行日志                                                    │   │
│   │                                                             │   │
│   │  10:30:15  AI 开始分析合同...                               │   │
│   │  10:30:18  规则命中：违约金条款检查                          │   │
│   │  10:30:19  规则命中：付款条件完整性检查                       │   │
│   │  10:30:21  AI 分析完成，发现 4 个风险项                      │   │
│   │  10:32:45  用户选择修复方案 B（付款条件）                    │   │
│   │  10:32:46  生成合同补丁 v1.1                                │   │
│   │                                                             │   │
│   └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.2 风险项状态机

```typescript
type RiskItemStatus = 
  | 'open'           // 待处理
  | 'fix_chosen'     // 已选择修复方案
  | 'fix_applied'    // 已应用修复
  | 'verified'       // 已验证
  | 'ignored';       // 已忽略

interface RiskItem {
  id: string;
  contractId: string;
  
  // 风险信息
  level: 'high' | 'medium' | 'low';
  title: string;
  description: string;
  
  // 证据
  evidence: {
    clauseRef: string;        // 条款引用（如"第5.2条"）
    originalText: string;     // 原文
    highlightRange?: [number, number];
  };
  
  // 修复方案
  fixes: Array<{
    id: string;
    label: string;
    description: string;
    newText: string;
    isRecommended: boolean;
  }>;
  
  // 状态
  status: RiskItemStatus;
  chosenFixId?: string;
  
  // 时间
  detectedAt: Date;
  resolvedAt?: Date;
}
```

---

## 六、统一 UI 组件规范

### 6.1 组件来源（强制）

| 来源 | 说明 |
|------|------|
| **shadcn/ui** | 基础组件唯一来源 |
| **@dhs-atlas/ui** | 业务组合组件 |

### 6.2 允许的自定义

| 类型 | 示例 |
|------|------|
| **主题 Tokens** | 颜色、圆角、间距、字体层级 |
| **组合式封装** | `DataTable`、`EntityHeader`、`Timeline` |
| **业务原语** | `QuoteEditor`、`ContractReview` |

### 6.3 禁止的行为

| 禁止行为 | 原因 |
|----------|------|
| 随意写一套新的 Button/Modal | 破坏一致性 |
| Emoji 作为图标或状态表达 | 不可解析、不专业 |
| 页面级魔改样式 | 破坏全局一致性 |
| 引入第二套 UI 库 | 维护成本高、体验不一致 |

---

## 七、体验验收清单

### 7.1 每个页面上线前必须检查

| # | 检查项 | 必要性 |
|---|--------|--------|
| 1 | 空态是否告诉用户下一步怎么做 | **必须** |
| 2 | Loading / Error 是否完整 | **必须** |
| 3 | 主 CTA 是否唯一且与状态机一致 | **必须** |
| 4 | 是否有 Timeline 或 Activity（关键对象） | **必须** |
| 5 | 每个动作是否有可追踪结果对象（job/event） | **必须** |
| 6 | 表单是否有自动保存/草稿（需要的地方） | 推荐 |
| 7 | 是否支持键盘操作与快捷键（至少保存/搜索） | 推荐 |
| 8 | 是否有批量操作（列表页） | 推荐 |
| 9 | 是否有权限/订阅原因提示（而不是直接消失） | **必须** |
| 10 | 是否所有交互都能被 AI Tool 驱动（有 action schema） | **必须** |

### 7.2 空态设计规范

```typescript
interface EmptyStateConfig {
  // 标题
  title: string;
  
  // 描述（告诉用户下一步）
  description: string;
  
  // 主动作
  action?: {
    label: string;
    href?: string;
    onClick?: () => void;
  };
  
  // 图标
  icon?: LucideIcon;
}

// 示例
const customerEmptyState: EmptyStateConfig = {
  title: '暂无客户',
  description: '创建您的第一个客户，开始管理客户关系',
  action: {
    label: '新建客户',
    href: '/customers/new',
  },
  icon: Users,
};
```

### 7.3 权限/订阅提示规范

```typescript
// ❌ 错误：直接隐藏按钮
{hasPermission && <Button>创建报价</Button>}

// ✅ 正确：显示原因
<Button 
  disabled={!hasPermission}
  title={!hasPermission ? '您没有此权限，请联系管理员' : undefined}
>
  创建报价
</Button>

// ✅ 更好：引导升级
{!hasFeature('quote.advanced') ? (
  <Button variant="outline" onClick={openUpgradeDialog}>
    <Lock className="mr-2 h-4 w-4" />
    高级报价（升级解锁）
  </Button>
) : (
  <Button>高级报价</Button>
)}
```

---

## 八、三个黄金样板页

### 8.1 Entity List 标准页

**适用**：客户/服务/菜单/项目/订单/合同等所有列表

**文件位置**：`packages/ui/src/templates/entity-list.tsx`

详见：[黄金样板页规范](./黄金样板页规范.md)

### 8.2 Entity Detail + Timeline 标准页

**适用**：所有实体详情页

**文件位置**：`packages/ui/src/templates/entity-detail.tsx`

详见：[黄金样板页规范](./黄金样板页规范.md)

### 8.3 Task Workbench 标准页

**适用**：合同预检/审批/验收/导入等任务型页面

**文件位置**：`packages/ui/src/templates/task-workbench.tsx`

详见：[黄金样板页规范](./黄金样板页规范.md)

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2025-12-28 | 初始版本 |

---

**相关文档**：
- [黄金样板页规范](./黄金样板页规范.md)
- [UI样式使用规范](./UI样式使用规范.md)
- [组件样式规范](./组件样式规范.md)

